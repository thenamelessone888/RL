# docs/

Reserved for the project's written documentation:

- `tmrl_integration.md` — the verified TMRL 0.7.1 integration points this project
  relies on (see the inspection notes already embedded as module docstrings in
  `td3/models.py`, `td3/agent.py`, `td3/config.py`, `demonstrations/human_interface.py`).
- `algorithm.md` — the TD3 implementation, and why it was selected over DDPG/other
  candidates (see `archive/ddpg_legacy/README.md` for the DDPG-to-TD3 rationale).
- `curriculum.md` — curriculum design once implemented (Section 10 of the research
  plan; not yet built).
- `experiments.md` — experiment log / results summary once baseline and curriculum
  experiments have been run (Sections 20-23).

Populated progressively as later phases complete rather than pre-written now.
